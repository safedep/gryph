// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/safedep/gryph/storage/ent/streamcheckpoint"
)

// StreamCheckpoint is the model entity for the StreamCheckpoint schema.
type StreamCheckpoint struct {
	config `json:"-"`
	// ID of the ent.
	// target_name used as primary key
	ID string `json:"id,omitempty"`
	// LastSyncedAt holds the value of the "last_synced_at" field.
	LastSyncedAt time.Time `json:"last_synced_at,omitempty"`
	// LastEventID holds the value of the "last_event_id" field.
	LastEventID string `json:"last_event_id,omitempty"`
	// LastSelfAuditID holds the value of the "last_self_audit_id" field.
	LastSelfAuditID string `json:"last_self_audit_id,omitempty"`
	selectValues    sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StreamCheckpoint) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case streamcheckpoint.FieldID, streamcheckpoint.FieldLastEventID, streamcheckpoint.FieldLastSelfAuditID:
			values[i] = new(sql.NullString)
		case streamcheckpoint.FieldLastSyncedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StreamCheckpoint fields.
func (_m *StreamCheckpoint) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case streamcheckpoint.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case streamcheckpoint.FieldLastSyncedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_synced_at", values[i])
			} else if value.Valid {
				_m.LastSyncedAt = value.Time
			}
		case streamcheckpoint.FieldLastEventID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_event_id", values[i])
			} else if value.Valid {
				_m.LastEventID = value.String
			}
		case streamcheckpoint.FieldLastSelfAuditID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_self_audit_id", values[i])
			} else if value.Valid {
				_m.LastSelfAuditID = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StreamCheckpoint.
// This includes values selected through modifiers, order, etc.
func (_m *StreamCheckpoint) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this StreamCheckpoint.
// Note that you need to call StreamCheckpoint.Unwrap() before calling this method if this StreamCheckpoint
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *StreamCheckpoint) Update() *StreamCheckpointUpdateOne {
	return NewStreamCheckpointClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the StreamCheckpoint entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *StreamCheckpoint) Unwrap() *StreamCheckpoint {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: StreamCheckpoint is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *StreamCheckpoint) String() string {
	var builder strings.Builder
	builder.WriteString("StreamCheckpoint(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("last_synced_at=")
	builder.WriteString(_m.LastSyncedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_event_id=")
	builder.WriteString(_m.LastEventID)
	builder.WriteString(", ")
	builder.WriteString("last_self_audit_id=")
	builder.WriteString(_m.LastSelfAuditID)
	builder.WriteByte(')')
	return builder.String()
}

// StreamCheckpoints is a parsable slice of StreamCheckpoint.
type StreamCheckpoints []*StreamCheckpoint
